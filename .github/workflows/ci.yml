name: ci
on:
  push:
    branches: [main, master]
  pull_request:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm install
      - run: npm run typecheck
      - run: npm run build
      - name: MCP stdio end-to-end test
        run: node test/mcp-stdio.test.mjs
      - name: Smoke-test the demo
        run: node dist/index.js --demo
